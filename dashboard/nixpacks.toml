# Dashboard nixpacks config - Static SPA
[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run generate"]

[start]
cmd = "npx serve .output/public -l $PORT"

