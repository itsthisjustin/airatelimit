# Dashboard nixpacks config
[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

# Note: start command must be set in Railway UI due to monorepo limitation
# Set to: node .output/server/index.mjs

